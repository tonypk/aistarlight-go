// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tax_drafts.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getLatestTaxDraft = `-- name: GetLatestTaxDraft :one
SELECT id, company_id, form_type, period_start, period_end, result, triggered_by, created_at FROM tax_drafts
WHERE company_id = $1 AND form_type = $2
ORDER BY period_end DESC
LIMIT 1
`

type GetLatestTaxDraftParams struct {
	CompanyID uuid.UUID `json:"company_id"`
	FormType  string    `json:"form_type"`
}

func (q *Queries) GetLatestTaxDraft(ctx context.Context, arg GetLatestTaxDraftParams) (TaxDraft, error) {
	row := q.db.QueryRow(ctx, getLatestTaxDraft, arg.CompanyID, arg.FormType)
	var i TaxDraft
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.FormType,
		&i.PeriodStart,
		&i.PeriodEnd,
		&i.Result,
		&i.TriggeredBy,
		&i.CreatedAt,
	)
	return i, err
}

const getTaxDraftByPeriod = `-- name: GetTaxDraftByPeriod :one
SELECT id, company_id, form_type, period_start, period_end, result, triggered_by, created_at FROM tax_drafts
WHERE company_id = $1 AND form_type = $2 AND period_start = $3 AND period_end = $4
`

type GetTaxDraftByPeriodParams struct {
	CompanyID   uuid.UUID   `json:"company_id"`
	FormType    string      `json:"form_type"`
	PeriodStart pgtype.Date `json:"period_start"`
	PeriodEnd   pgtype.Date `json:"period_end"`
}

func (q *Queries) GetTaxDraftByPeriod(ctx context.Context, arg GetTaxDraftByPeriodParams) (TaxDraft, error) {
	row := q.db.QueryRow(ctx, getTaxDraftByPeriod,
		arg.CompanyID,
		arg.FormType,
		arg.PeriodStart,
		arg.PeriodEnd,
	)
	var i TaxDraft
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.FormType,
		&i.PeriodStart,
		&i.PeriodEnd,
		&i.Result,
		&i.TriggeredBy,
		&i.CreatedAt,
	)
	return i, err
}

const upsertTaxDraft = `-- name: UpsertTaxDraft :one
INSERT INTO tax_drafts (company_id, form_type, period_start, period_end, result, triggered_by)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (company_id, form_type, period_start, period_end)
DO UPDATE SET result = $5, triggered_by = $6, created_at = now()
RETURNING id, company_id, form_type, period_start, period_end, result, triggered_by, created_at
`

type UpsertTaxDraftParams struct {
	CompanyID   uuid.UUID   `json:"company_id"`
	FormType    string      `json:"form_type"`
	PeriodStart pgtype.Date `json:"period_start"`
	PeriodEnd   pgtype.Date `json:"period_end"`
	Result      []byte      `json:"result"`
	TriggeredBy *string     `json:"triggered_by"`
}

func (q *Queries) UpsertTaxDraft(ctx context.Context, arg UpsertTaxDraftParams) (TaxDraft, error) {
	row := q.db.QueryRow(ctx, upsertTaxDraft,
		arg.CompanyID,
		arg.FormType,
		arg.PeriodStart,
		arg.PeriodEnd,
		arg.Result,
		arg.TriggeredBy,
	)
	var i TaxDraft
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.FormType,
		&i.PeriodStart,
		&i.PeriodEnd,
		&i.Result,
		&i.TriggeredBy,
		&i.CreatedAt,
	)
	return i, err
}
